name: flutter_hybrid_template
description: A hybrid Flutter app for Mr Ahmed Gamal platform.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_inappwebview: ^6.0.0
  onesignal_flutter: ^5.2.2
  url_launcher: ^6.2.5 # المكتبة المسؤولة عن فتح الواتساب والاتصال

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
